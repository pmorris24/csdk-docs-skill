Answer the following question about the Sisense Compose SDK using the documentation files in the `docs/` directory of this project.

## Instructions

1. First, determine which framework the user is asking about (React, Vue, or Angular) from their question or the current project context. Default to React if unclear.

2. Read the relevant documentation files from the `docs/` folder:
   - **Always read**: `csdk_guides.md`, `csdk_api_sdk_data.md`, `csdk_full_compact.md`
   - **React**: also read `csdk_api_sdk_ui.md`
   - **Vue**: also read `csdk_api_sdk_ui_vue.md`
   - **Angular**: also read `csdk_api_sdk_ui_angular.md`
   - For styling/design questions: also read `csdk_chart_design.md` and `csdk_ui_ux_skill.md`
   - For supplemental context: `csdk_supplemental.md`

3. Use the documentation to provide an accurate, detailed answer. Include code examples from the docs when relevant.

4. If the answer requires information not found in the docs, say so clearly.

## Question

$ARGUMENTS
